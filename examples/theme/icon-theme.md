# Icon Theme Reference

Example third-party icon theme install:

```bash
git clone --depth 1 https://github.com/vinceliuice/MacTahoe-icon-theme.git
cd MacTahoe-icon-theme
./install.sh
```

Review upstream instructions before running; theme installers can change desktop settings.
